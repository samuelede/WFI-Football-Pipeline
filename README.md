# WFI-Football-Pipeline
Automated ELT pipeline that extracts World Cup data from football-data.org, lands it in GCS, and loads it into a three-layer BigQuery warehouse (raw, staging, gold) orchestrated with Airflow on Docker.
